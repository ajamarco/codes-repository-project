<%= form_for @post do |form|%>

<%= form.label :title%>
<%= form.text_field :title%><br>

<%= form.label :content%>
<%= form.text_field :content%>

<%= select(:post, :user_id, @user_option)%>

<%= form.submit "Save post"%>

<%end%>