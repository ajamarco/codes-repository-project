<%= form_for @post do |f|%>
    <%= f.label :title%><br>
    <%= f.text_field :title%><br>

    <%= f.label :content%><br>
    <%= f.text_area :content%><br>

    <%= select(:post, :user_id, @user_options) %>
   <%# #<%= f.select (:user_id, @users.map{|u| u.name, u.id  }, :selected => user.id)  %>
   

    <%= f.submit "Save new posts"%>
<%end%>
