<!DOCTYPE html>
<html>
  <head>
    <title>CodesRepositoryProject</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav>
      <a href="/tags/new">Create New Tag</a> ||
      <a href="/users">all the users</a> ||
      <a href="/users/new">Create a new User</a> ||
      <a href="/posts/new">Create a new post</a> ||
      <a href="/posts">all the posts</a> ||

      <%= form_tag({controller: 'searchs', action: 'search_posts', method: 'post'}, {class: 'search-form'}) do %>
        <input name='title' class="form-control mr-sm-2" type="search" placeholder="Search for Posts" aria-label="Search">
      <% end %>

      <%= form_tag({controller: 'searchs', action: 'search_posts', method: 'post'}, {class: 'search-form'}) do %>
        <input name='title' class="form-control mr-sm-2" type="search" placeholder="Search for Posts" aria-label="Search">
      <% end %>

    </nav>
    <%= yield %>
  </body>
</html>
